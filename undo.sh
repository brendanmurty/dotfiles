# Revert to the previous versions of the user configuration files
sudo mv ~/.bash_aliases.bak ~/.bash_aliases
sudo mv ~/.bash_prompt.bak ~/.bash_prompt
sudo mv ~/.bashrc.bak ~/.bashrc
sudo mv ~/.gitconfig.bak ~/.gitconfig
sudo mv ~/.gitignore.bak ~/.gitignore
sudo mv ~/.vimrc.bak ~/.vimrc
